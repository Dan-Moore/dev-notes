# Hello World - Monorepo with Nextjs & Turbo
todo - monolith write up

## Init - Creating A Skeleton Project
https://ui.shadcn.com/docs/monorepo
```
pnpm dlx shadcn@canary init
```

## shadcn
```
cd apps/web 

pnpm dlx shadcn@canary add
pnpm dlx shadcn@canary add tooltip sidebar separator
pnpm dlx shadcn@canary add dropdown-menu avatar badge breadcrumb card carousel chart checkbox calendar collapsible command context-menu dialog drawer hover-card label menubar navigation-menu pagination popover progress radio-group resizable scroll-area select switch table tabs textarea toggle toggle-group
```


## MDX 
https://nextjs.org/docs/app/guides/mdx
https://github.com/hashicorp/next-mdx-remote

### Packages
Install next-mdx & next-mdx-remote

```
npm install @next/mdx @mdx-js/loader @mdx-js/react @types/mdx next-mdx-remote
```

### update next.config.mjs
```mjs
import createMDX from '@next/mdx'
/** @type {import('next').NextConfig} */
const nextConfig = {
  pageExtensions: ['js', 'jsx', 'md', 'mdx', 'ts', 'tsx'],
}
 
const withMDX = createMDX({
})

export default withMDX(nextConfig)
```

### make mdx-components.tsx
```tsx
import type { MDXComponents } from 'mdx/types'

export function useMDXComponents(components: MDXComponents): MDXComponents {
  
  return {
    ...components, 
  }
}
```